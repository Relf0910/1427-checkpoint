interface:
  display_name: "Checkpoint"
  short_description: "Session checkpoint that halts at the checkpoint and shows change frameworks before edits"
  default_prompt: "Use $1427-checkpoint as HARDENED checkpoint to monitor this session and display a change framework before any code modification — halted at the checkpoint until explicit approval. Proceed only after I explicitly approve. Package or install only after I explicitly choose that action."
